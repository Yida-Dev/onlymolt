{
  "name": "onlymolt-site",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "build": "node scripts/build.js",
    "deploy": "npm run build && wrangler pages deploy dist --project-name=onlymolt"
  }
}
